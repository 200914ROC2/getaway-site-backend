pipeline {

    agent any
    stages {

        stage('stop old app') {
            steps {

                echo 'need to shut down server'
            }
        }

        stage('start up server again'){
            echo 'need to start up server'
        }
    }
}